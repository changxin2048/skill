# 手绘 SVG 完整配方（Sketch Recipe）

生成前必读。本文档自包含全部坐标、参数、色板与结构示例，**写代码前只需读本文件**，无需读取 `assets/` 下的示例 SVG（`eg.svg`/`eg2.svg` 仅供维护者人工核对，不是生成输入）。

---

## 0. 随机化总则（每次生成前必做）

手绘风的魅力在于"每一张都是独一份"。**每次生成都必须与之前的图不同**（同一需求重复两次，至少 3 个维度可感知地不同；配色几乎不可能重复）。

随机是**受控随机**：只从本文各章节的候选池/范围内取值，保证风格始终协调。配色方向固定为两大色系：**莫兰迪（灰调低饱和）** 与 **马卡龙（粉嫩低饱和高亮）**，每次二选一，然后让每个层独立掷色。

**掷骰子流程**（写代码之前完成）：
1. **掷色系**：莫兰迪 / 马卡龙（约 50/50）。
2. **逐层掷色**：7 个层（用户/编排/LLM/工具/记忆/知识/安全）从该色系 8 色池中**每层独立随机取 1 色相**，相邻两层不取同一色相（撞色就换）。LLM 是实心层，取该色相深档做填充。
3. **掷纸色**：从该色系对应纸色池（见 8.4）选 1 个。
4. **掷滤镜**：seed/scale/baseFrequency 随机（两个 seed 必须不同）。
5. **掷其余**：排线角度/透明度、箭头主色、虚线样式、装饰元素（见各节）。
6. 把全部已选参数写成 SVG 顶部注释：
   `<!-- 本次随机参数：色系=莫兰迪；层色=灰粉/灰绿/灰蓝/燕麦/陶土；纸色=#F0ECE6；seedA=31/scaleA=3.4；seedB=72/scaleB=4.0；排线=45°/0.12；箭头=#8E5F47；装饰=星+太阳+圆点；虚线="5,4" -->`

然后按这些参数写代码。自检：与上次生成的图对比，至少 3 个维度不同，且配色组合不同。

---

## 1. 文件骨架

```xml
<svg xmlns="http://www.w3.org/2000/svg" width="1100" height="960" viewBox="0 0 1100 960">
  <style>
    @import url('https://fonts.googleapis.com/css2?family=ZCOOL+KuaiLe&amp;family=Liu+Jian+Mao+Cao&amp;display=swap');
    .text { font-family: 'ZCOOL KuaiLe','Liu Jian Mao Cao','Kaiti SC','STKaiti','KaiTi','Comic Sans MS','Bradley Hand',cursive; }
    .shapes { stroke-linecap: round; stroke-linejoin: round; }
  </style>
  <!-- 本次随机参数：…（见第 0 节） -->
  <defs>…滤镜与 pattern…</defs>
  <!-- 纸张背景 -->
  <!-- 形状组：<g filter="url(#sketchA)" class="shapes">（矩形/小块/涂鸦装饰，不含连接线） -->
  <!-- 描边组：<g filter="url(#sketchB)" class="shapes">（大容器第二遍描边；单描边模式时整组删除） -->
  <!-- 连接线组：<g class="shapes">（流程线/连接线/箭头/编号圆圈，不使用任何滤镜） -->
  <!-- 文字组：<g class="text"> -->
</svg>
```

## 2. 抖动滤镜（两个，seed 每次不同）

```xml
<filter id="sketchA" x="-8%" y="-8%" width="116%" height="116%">
  <feTurbulence type="fractalNoise" baseFrequency="0.06" numOctaves="3" seed="7" result="n"/>
  <feDisplacementMap in="SourceGraphic" in2="n" scale="3.4" xChannelSelector="R" yChannelSelector="G"/>
</filter>
<filter id="sketchB" x="-8%" y="-8%" width="116%" height="116%">
  <feTurbulence type="fractalNoise" baseFrequency="0.055" numOctaves="3" seed="13" result="n"/>
  <feDisplacementMap in="SourceGraphic" in2="n" scale="4" xChannelSelector="R" yChannelSelector="G"/>
</filter>
```

**随机规则**（用户已验证偏好：位移幅度要小、噪声频率要高——滤镜只做轻微"手抖"，不做大幅扭曲）：
- `seed`：A 与 B 各取 1~199 内不同整数（每次都要换，这是抖动形态随机的关键）。
- `scale`：A 取 2.5~4.5，B 取 3~5.5（B 略大于 A）。**3 上下为佳**，超过 5 就太扭曲。
- `baseFrequency`：A 取 0.05~0.07，B 取 0.045~0.065。**0.055~0.06 为佳**，低于 0.04 则抖动呈大波浪、不像笔迹。
- 滤镜区域固定放大到 116%，防止边缘被裁。

## 3. 双描边叠线（马克笔画两笔的效果）

每个**大容器**画三层：
1. 填充 rect（sketchA 组）
2. 第一遍描边 rect，`stroke-width` 取 2.5~4（sketchA 组）
3. 第二遍描边 rect，`stroke-width` 取 1.3~2，颜色可同色相深一档（sketchB 组，seed 不同 → 与第一遍错开）

**内部小块**只画一遍描边（stroke-width 1.8~2.4），避免杂乱。圆角：容器 rx 8~18，小块 rx 5~12（每次在范围内随机取整）。

### 3.1 单描边模式（用户要求"单描边 / 不要叠线"时）

默认双描边。若用户明确要求单描边：
1. 删掉整个**描边组**（sketchB 组里的全部第二遍描边 rect）；
2. 同时删掉 `<defs>` 里的 sketchB 滤镜定义（避免死代码）；
3. 只保留 sketchA 组内的主描边（大容器 2.5~4，小块 1.8~2.4）。

## 4. 纸张背景（色池 + 点阵随机）

纸色随色系（见 8.4）。点阵色用纸色同色系深一档。

```xml
<pattern id="paperDots" width="22" height="22" patternUnits="userSpaceOnUse">
  <circle cx="3" cy="3" r="1.3" fill="#cbbfa9" opacity="0.5"/>
</pattern>
<rect width="1100" height="960" fill="<!-- 选定的纸色 -->"/>
<rect width="1100" height="960" fill="url(#paperDots)"/>
```

**点阵随机**：`width/height` 取 16~28，`r` 取 1.0~1.6，`opacity` 取 0.35~0.65。

## 5. 铅笔排线（叠加在大容器填充之上）

```xml
<pattern id="hatch" width="8" height="8" patternUnits="userSpaceOnUse" patternTransform="rotate(45)">
  <line x1="0" y1="0" x2="0" y2="8" stroke="#047857" stroke-width="1" opacity="0.15"/>
</pattern>
```

**随机规则**：`rotate` 取 15/30/45/60/75 之一；`opacity` 取 0.08~0.22；`stroke-width` 取 0.8~1.3。每层一个 pattern，`stroke` 用该层"排线"档。用法：`<rect … fill="url(#hatch)"/>` 叠在填充 rect 之后。

## 6. 手绘箭头（不用 marker，直接画不规则三角形）

```xml
<!-- 向下箭头 -->
<path d="M543,232 L557,234 L550,244 Z" fill="#f59e0b"/>
<!-- 向上箭头 -->
<path d="M193,551 L207,553 L200,542 Z"/>
```

**随机规则**：底边两端点高度差 1~3px、顶点偏离中线 1~2px（随手感），具体偏移量每次不同。不要画等腰标准三角形。箭头颜色 = 第 7 节箭头主色。**箭头与流程线/连接线同组，不套任何滤镜**（见第 7 节）。

## 7. 连接线

- **流程线/连接线/箭头不使用任何滤镜**（用户已验证偏好）：单独一组 `<g class="shapes">`，置于所有滤镜组之外。滤镜只作用于矩形与涂鸦，线条保持清晰利落。
- 主连接用轻微 S 形三次曲线：`M550,198 C547,208 553,222 550,233`（中点偏移 3~6px，每次不同）。
- 返回/辅助路径用虚线，`stroke-dasharray` 在 `"7,6"`/`"6,5"`/`"8,6"`/`"5,4"` 中随机，颜色 `#94a3b8`。
- 分支线：先垂直一小段 → 水平段 → 垂直进目标。
- **弯折处一律圆角过渡，禁止生硬直角**（用户已验证偏好）：所有 L 形 90° 转弯用 Q 曲线（半径 3~6px）；T 形汇入点（横线接竖线汇流）可直连。例：
```xml
<!-- 生硬直角（不要） -->
<path d="M300,300 L300,352 L352,352"/>
<!-- 圆角弯折（要）：Q 曲线，半径约 4px -->
<path d="M300,300 L300,348 Q300,352 304,352 L352,352"/>
```

### 连接线与边框的关系（铁律，踩坑沉淀）

1. **分支线绝不能沿图形边框走**。竖线贴着块左边框上下延伸 = 线条与边框共线重合，视觉上像画错了。正确做法：分支竖线走**偏移通道**（例：块左边框在 x:750，竖线走 x:730），到目标高度后接**短横线**，箭头在边框处接入块内。
2. **线条起止点不要恰好落在边框上**。无箭头的一端离边框留 3~5px 间隙；有箭头的一端让箭头尖端落在边框上、线体停在箭头之后。也可让线从块**内部**引出、垂直穿过边框。
3. **同一侧边框的多组线要在垂直方向错开**。如"调用进入"与"结果返回"都接在块的左边缘：进入点取 y:258/438/618、返回点取 y:290/470/650——两套线永无共线段。
4. 垂直交叉（实线×虚线）可接受且正常；**共线/重合**不可接受。画完自查：任意线条与任意边框不得有超过一个点的接触。

**箭头主色池（随色系，每次随机选 1 个）**：
- 莫兰迪：深灰蓝 `#4E6570` / 陶土 `#8E5F47` / 雾紫 `#715F7A` / 石墨 `#5F5F5F` / 灰芥 `#83794C`
- 马卡龙：玫红 `#D9738A` / 天蓝 `#6E9ECB` / 淡紫 `#9678C0` / 蜜橙 `#DB9066` / 青绿 `#6EB5B4`

## 8. 色板（核心：双色系 × 每层独立掷色）

### 8.1 莫兰迪色系（灰调、低饱和、柔和）

每色 4 档：`描边深 / 填充浅 / 排线更深 / 小块描边`。

| 色名 | 描边 | 填充 | 排线 | 小块 |
|---|---|---|---|---|
| 灰蓝 Dusty Blue | `#7A919E` | `#DDE5E8` | `#4E6570` | `#B9CBD1` |
| 灰粉 Dusty Rose | `#B58B93` | `#F0E2E4` | `#8A626C` | `#DCC2C7` |
| 灰绿 Sage | `#8FA38F` | `#E4EAE2` | `#5F7560` | `#C6D4C5` |
| 燕麦 Oat | `#B8A58E` | `#EFE7DB` | `#86745E` | `#DDCFBC` |
| 陶土 Terracotta | `#BC8A6F` | `#F2E5DC` | `#8E5F47` | `#E0C4B2` |
| 雾紫 Mauve | `#A08FA8` | `#E9E2EC` | `#715F7A` | `#D0C4D6` |
| 灰芥 Olive Grey | `#B3A877` | `#EFEBDA` | `#83794C` | `#DAD2AC` |
| 石墨 Graphite | `#8E8E8E` | `#E6E4E0` | `#5F5F5F` | `#C8C6C2` |

### 8.2 马卡龙色系（低饱和、高亮度）

| 色名 | 描边 | 填充 | 排线 | 小块 |
|---|---|---|---|---|
| 樱花粉 Macaron Pink | `#F2A9B8` | `#FDECEF` | `#D9738A` | `#F8CDD5` |
| 薄荷绿 Mint | `#A8D8C5` | `#EBF7F2` | `#6FB39B` | `#CEEADF` |
| 柠檬黄 Lemon | `#F4D58C` | `#FEF7E4` | `#D9AE4E` | `#FAE9BE` |
| 天空蓝 Sky | `#A9CBE8` | `#ECF4FB` | `#6E9ECB` | `#D2E4F3` |
| 淡紫 Lavender | `#C7B4E3` | `#F4EFFA` | `#9678C0` | `#E2D7F1` |
| 蜜桃 Peach | `#F5C3A8` | `#FEF1E8` | `#DB9066` | `#FAE0CE` |
| 奶黄 Butter | `#E8D9A8` | `#FCF8E9` | `#C4AC60` | `#F2E8C6` |
| 淡青 Aqua | `#A5DCDB` | `#ECF8F7` | `#6EB5B4` | `#D1EEED` |

### 8.3 分配规则（每层独立骰子）

- 先掷色系（莫兰迪 / 马卡龙），整张图只用该色系。
- 用户层、编排层、工具层、记忆层、知识层、安全层：各从 8 色池**独立随机取 1 色相**；相邻两层不取同一色相（撞色就换一个）。
- **LLM 层是实心层**：填充取所选色相的"描边深"档，内描边取同色"排线"档（更深）。
- 排线 pattern 用该层"排线"档，小块描边用"小块"档；小块填充统一 `#fffdf7`。
- 层标题文字用该层"排线"档（更深的同色相），块标题 `#334155`，说明 `#64748b`。
- 流程编号用手绘圆圈 + 阿拉伯数字，不要默认的实心圆点；不同层级编号的样式区分规则见 8.5（必做）。

### 8.4 纸色池（随色系，每次随机选 1 个）

- **莫兰迪**：暖灰白 `#F0ECE6` / 燕麦 `#F3EEE4` / 冷灰白 `#ECEFED` / 浅灰粉 `#F2ECEB` / 浅灰绿 `#EEF0EA`
- **马卡龙**：奶油 `#FBF7EC` / 浅粉白 `#FDF2F4` / 浅蓝白 `#F0F6FA` / 浅黄白 `#FCF7E6` / 浅绿白 `#EFF7F2`

### 8.5 编号样式区分（必做，踩坑沉淀）

一张图里若存在**不同层级或不同含义的编号序列**，样式必须可感知地区分，否则读者分不清哪个是主流程、哪个是内部步骤。

**默认规则（用户已验证偏好）**：
- 序号统一用**阿拉伯数字**（1、2、3…），不用圈号 ①②③（部分字体/预览工具下圈号字形不稳定）。
- **主流程步骤**：实心圆 + 白字（fill = 箭头主色，stroke 用同色深一档）。如 `1 用户请求 → 2 推理请求 → …`
- **循环/内部流程步骤**：少于等于3个时不使用编号，多余3个使用空心圆+编号
- **返回/辅助路径**：灰色实心圆（fill `#94a3b8` + 白字，或空心灰圈）+ 灰色虚线连接，标注文字用灰字。
- 不同序列的数字可以重复（主流程 1-5 与内部 1/2/3 共存），靠**实心/空心/颜色**区分；若仍易混，可把次要序列换成中文/罗马数字，但默认优先阿拉伯数字 + 样式区分。
- 编号圆圈旁紧跟简短标注文字（如 `1 用户请求`），颜色与圆圈语义一致；分支处行内标签（如 `调用工具`）可不带编号，避免与主编号文字重复。

**避免重叠（踩坑沉淀）**：
- 多个编号集中在同一区域时（如流程线密集区），先算好每个圆圈 + 标注文字的包围范围再落位：圆圈错开 y、文字缩短、必要时调整 text-anchor（如返回线编号文字右对齐到竖线左侧）。
- 编号圆圈不得压在连接线/虚线/容器边框上（r=11 的圆圈至少留 5px 间隙）。
- 实心圆内的数字必须用白字，空心圆内的数字必须用深色字（同色相深档），保证对比度。

示例 XML（实心主流程 +灰色辅助）：
```xml
<!-- 主流程：实心青绿圈 -->
<circle cx="556" cy="206" r="11" fill="#6EB5B4" stroke="#4A9E9D" stroke-width="1.6"/>
<text x="556" y="210" text-anchor="middle" font-size="12" font-weight="700" fill="#ffffff">1</text>

<!-- 返回/辅助：灰色实心圈 -->
<circle cx="820" cy="552" r="11" fill="#94a3b8" stroke="#64748b" stroke-width="1.6"/>
<text x="820" y="556" text-anchor="middle" font-size="12" font-weight="700" fill="#ffffff">4</text>
```

## 9. 字号与排版

- 大标题 26~32 / 层标题 15~16 / 块标题 12.5~13 / 说明 10~11.5（在范围内每次取不同值）。
- 中英对照：中文主 + `<tspan>` 英文副（英文降一档字号、透明度 0.75）。
- 容器标签放左上角 (x+18, y+26) 左右；块内文字居中。

## 10. 装饰元素（随机 2~5 个，别喧宾夺主）

元素池（每次从池中随机挑 2~5 个组合）：

```xml
<!-- 四角星 -->
<path d="M0,-12 C1,-3 3,-1 12,0 C3,1 1,3 0,12 C-1,3 -3,1 -12,0 C-3,-1 -1,-3 0,-12 Z"
      transform="translate(150,115) scale(1.1)" fill="#f472b6" opacity="0.85"/>
<!-- 小圆点 -->
<circle cx="945" cy="520" r="6" fill="#fbbf24" opacity="0.7"/>
<!-- 小螺旋（1.5 圈） -->
<path d="M960,95 m0,0 c4,-6 12,-4 13,2 c1,6 -6,9 -11,5 c-4,-3 -4,-9 1,-11 c4,-2 8,1 7,5"
      fill="none" stroke="#f472b6" stroke-width="2"/>
<!-- 小太阳（圆 + 短射线） -->
<g transform="translate(150,560)">
  <circle r="7" fill="#fbbf24"/>
  <path d="M0,-11 L0,-8 M0,11 L0,8 M-11,0 L-8,0 M11,0 L8,0 M-8,-8 L-6,-6 M8,8 L6,6 M8,-8 L6,-6 M-8,8 L-6,6"
        stroke="#fbbf24" stroke-width="1.8" fill="none"/>
</g>
<!-- 标题波浪下划线：约 60% 概率画；画时 1~2 条，振幅 5~9px -->
<path d="M330,60 C370,53 410,67 450,60 C490,53 530,67 570,60 C610,53 650,67 690,60 C730,53 770,67 810,60"
      fill="none" stroke="<!-- 箭头主色 -->" stroke-width="2~3"/>
```

**随机规则**：
- 数量 2~5 个；位置在四角附近随机偏移 20~40px；`scale` 0.5~1.3。
- 颜色从亮色池选：`#f472b6` / `#fbbf24` / `#22d3ee` / `#a78bfa` / 马卡龙色系深档（莫兰迪主题可混入少量亮色点缀，反差更俏皮）。

## 11. 避坑清单（每条都是踩过的坑）

1. **CSS `@import` 里的 `&` 必须写成 `&amp;`**，否则 XML 解析报 `not well-formed (invalid token)`。
2. **文字绝不能进滤镜组**——会糊成一团。形状组和文字组必须分离。
3. 滤镜区域必须扩大（`x="-8%" y="-8%" width="116%" height="116%"`），否则边缘被裁。
4. 双描边必须用**不同 seed** 的两个滤镜，否则两遍描边完全重合等于没画。
5. SVG 无自动排版，每个元素都要显式坐标；先算好布局再写代码。
6. 文件保存为 UTF-8，中文直接书写，无需转义。
7. 生成后**必须**用 XML 解析校验（见下），再交付。
8. **外层容器边框不能与内部小图形边框重合**：内部小块四周必须留边距——底边至少 8~14px、左右至少 10~14px、顶部位于层标题之下；等距排布 N 个小块时先算好坐标，让最后一块的底边明显高于容器底边框（例：块高 160 内放 3 条，条高 32 + 间距 8 + 底部留 14）。连接线尽量穿过小块之间的缝隙。
9. **连接线不得沿边框共线**：分支竖线贴块边 = 重合，必须走偏移通道 + 短横线 + 箭头接入；线体起止点留 3~5px 间隙或从块内垂直穿出；同一边框多组线垂直错开（详见第 7 节"连接线与边框的关系"）。
10. **单描边模式**：用户要求单描边时，删除整个 sketchB 描边组与 sketchB 滤镜定义，只留 sketchA 主描边（详见第 3.1 节）。
11. **连接线不套滤镜**：流程线/连接线/箭头/编号圆圈单独成组，置于所有滤镜组之外（滤镜只作用于矩形与涂鸦）；套了滤镜的线条会随图形一起抖，违背"线条清晰"的要求。画完自查：连接线组内不应出现 `filter=` 属性。
12. **弯折处不用生硬直角**：所有 L 形 90° 转弯用 Q 曲线圆角过渡（半径 3~6px）；T 形汇入点可直连。逐条检查路径里的 `L` 拐角，有直角就换成 `Q`。

## 12. 校验与随机性自检

```bash
# XML 校验
python3 -c "import xml.etree.ElementTree as ET; ET.parse('输出.svg'); print('OK')"

# 渲染 PNG 预览（自动处理 cairosvg 缺失；脚本路径为 Skill 资源目录绝对路径，勿按当前工作目录解析）
python3 /Users/Admin/.agents/skills/handdrawn-svg/scripts/render_preview.py 输出.svg
```

**随机性自检**（交付前）：对比上一次生成的图，确认以下维度中至少 3 个不同，且**每层色相组合不同**：
色系（莫兰迪/马卡龙）/ 各层色相 / 纸色 / 滤镜 seed / scale / 排线角度 / 箭头主色 / 装饰元素组合 / 圆角与线宽。

预览图字体可能走回退字体（cairosvg 不加载网页字体），告知用户以浏览器打开 SVG 为准。

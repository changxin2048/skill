---
name: handdrawn-svg
description: 生成手绘/涂鸦/马克笔风格（hand-drawn, sketchy, marker style）的 SVG 矢量图：架构图、系统图、流程图、结构图、思维导图等，也可把用户已有的普通 SVG 框图重绘成手绘风。当用户提到"手绘风""手账风""涂鸦风""sketch 风""像手画的""马克笔效果""更随性/别太死板""hand-drawn""wobbly""莫兰迪""马卡龙"等字眼，或要求把架构图/流程图改造成手绘风格时，务必使用本 Skill。产出带手写字体、抖动线条、双描边叠线、网格纸背景的手绘 SVG，配色每次随机（莫兰迪灰调 / 马卡龙粉嫩色系，各层独立掷色），可选渲染 PNG 预览。注意：用户要的是普通正式风格（整齐、无滤镜）的 SVG 时不要使用本 Skill。
---

# 手绘风 SVG 生成器 (Hand-Drawn SVG)

把结构化的框图（架构图 / 流程图 / 思维导图 / 系统图等）变成"像用马克笔在网格纸上手绘"的 SVG 矢量图。

## ⚠️ 路径说明（必读，先看这里再动手）

本 Skill 内所有相对路径（`references/sketch_recipe.md`、`scripts/render_preview.py`、`assets/`）**都相对于 Skill 资源目录**，不一定是当前工作目录！

- 资源目录 = 调用 `skill` 工具时返回的 `resourceBase.path`，

**禁止**把相对路径按当前工作目录解析（会报 not found）。用 `read` / `glob` / `bash` 访问上述文件时，一律拼上资源目录的绝对路径；调用渲染脚本同理（见工作流第 6 步）。

## 核心思路（先理解 Why）

手绘感 = **笔迹抖动 + 手写字体 + 纸张质感 + 不完美的线条**，四者缺一效果都不对。不要画完美直线、直角、正圆——所有"生硬感"都要靠滤镜和字形消除。结构内容由用户需求决定，风格统一由本 Skill 决定。

## 工作流

1. **解析需求**：确定图的类型、层/模块、连接关系、文案。布局与内容跟着用户需求走。
2. **布局**：画布建议 1100×960（可等比缩放）。自上而下分层：大容器包小块。每个元素都要精确坐标。布局与坐标直接参照 `references/sketch_recipe.md` 内嵌示例即可，**无需读取 `assets/` 下的示例 SVG 文件**。
3. **掷骰子（随机化）**：写代码前，先按 `references/sketch_recipe.md` 的「随机化」章节为本次生成确定全部随机参数——主题色板、纸张背景色、滤镜 seed 与 scale、排线角度/透明度、连接线颜色、涂鸦配置等。把选定的参数以注释形式写进 SVG 顶部（`<!-- 本次随机参数：… -->`），便于复核。
4. **生成 SVG**：按 `references/sketch_recipe.md` 的配方与本次已选随机参数写。文件结构固定为：
   - `<defs>`：两个抖动滤镜 `sketchA`/`sketchB`（**不同 seed**）、纸张点阵 pattern、各层排线 pattern、CSS `@import` 手写字体
   - 纸张背景 rect（米黄底 + 点阵）
   - **形状组** `<g filter="url(#sketchA)">`：所有矩形、小块、涂鸦装饰（**不含连接线**）
   - **描边组** `<g filter="url(#sketchB)">`：各大容器的第二遍描边（与第一遍错开形成"画了两笔"的叠线；用户要求单描边时删除本组及其滤镜定义，见 recipe 第 3.1 节）
   - **连接线组** `<g class="shapes">`：流程线/连接线/箭头/编号圆圈，**不使用任何滤镜**；弯折处用圆角过渡，禁止生硬直角
   - **文字组**（无滤镜）：所有文字
5. **校验 XML**：用 `python3 -c "import xml.etree.ElementTree as ET; ET.parse('文件')"` 确认合法。最常见坑：CSS `@import` 里的 `&` 必须写成 `&amp;`。
6. **渲染预览（可选）**：运行 `python3 /Users/Admin/.agents/skills/handdrawn-svg/scripts/render_preview.py <svg>` 生成 PNG（cairosvg 缺失时自动建 venv 安装，用户环境不允许往全局 site-packages 装包时也能用；脚本路径必须用资源目录绝对路径，见「路径说明」）。
7. **交付**：给出 `.svg` 路径；有预览则同时给出 `.png` 路径，并说明"预览图字体可能走回退字体，以浏览器打开 SVG 为准"。

## 关键规则

- **文字永远不进抖动滤镜**（会糊掉）；矩形/小块/涂鸦进滤镜；**流程线/连接线/箭头不进任何滤镜**（保持线条清晰），单独成组。
- 字体族：`'ZCOOL KuaiLe','Liu Jian Mao Cao','Kaiti SC','STKaiti','KaiTi','Comic Sans MS','Bradley Hand',cursive`。在线时浏览器自动加载快乐体/草书，离线回退楷体/Comic Sans。
- 默认**不做微旋转**；只有当用户明确要"更随意、歪歪扭扭"时才给大容器加 -1.5°~1.5° 的 rotate。
- **每次生成必须不同**（铁律）：同一需求重复生成两次，至少 3 个维度要可感知地不同——背景色、主题色板、滤镜 seed/scale、排线角度、箭头色、涂鸦布局等。手绘风的价值就在于"每一张都是独一份"。但随机必须"受控"：只从 recipe 随机化章节给出的候选池里选，保证风格始终协调，不出现突兀的颜色。
- 配色每次随机：先掷色系（**莫兰迪灰调 / 马卡龙清新**二选一），再让各层从该色系 8 色池**独立掷色相**（相邻层不撞色）——组合空间极大，两张图几乎不可能同色。大容器浅色底 + 同色系排线 + 深色双描边；小块近白 `#fffdf7` 底。
- 连接线用轻微 S 形曲线 + 手绘箭头（不规则三角形，不要等腰标准三角形）；返回/辅助路径用虚线 `stroke-dasharray="7,6"`。
- **连接线不套滤镜**（单独成组，见工作流）；**弯折处一律圆角过渡（Q 曲线，半径 3~6px），禁止生硬直角**（T 形汇流可直连）。
- 连接线与边框的关系是铁律：分支线不得沿图形边框共线（走偏移通道 + 短横线 + 箭头接入）；线条起止点与边框留 3~5px 间隙或从块内垂直穿出；同一边框多组线垂直错开；外层容器边框不得与内部小图形边框重合（内部小块四周留边距），详见 recipe 第 7 节与避坑清单。
- 字号：大标题 26-30，层标题 15-16，块标题 12.5-13，说明 10-11.5；中英对照（中文主 + 英文副）提升专业感。
- 编号序列必须可区分（踩坑沉淀）：一张图里不同层级/含义的编号（主流程 / 返回辅助）要用不同圆圈样式并配图例。默认规则：序号统一用阿拉伯数字；主流程步骤=实心圆+白字；返回/辅助=灰色（配灰虚线）。不要用同一套样式标两套编号，也不要让编号圆圈/标注文字互相重叠（密集区域错位排布、缩短标注），详见 recipe 第 8.5 节。
- 可加少量涂鸦装饰（四角小星星、标题波浪下划线）增加手账感，但不要喧宾夺主。

## 参考与资产

- `references/sketch_recipe.md` — **唯一必读来源**。完整配方：滤镜参数、pattern 定义、手绘箭头、XML 片段、色板、**随机化规则（候选池）**、避坑清单、结构示例（生成前必读，且已内嵌全部所需布局参考）。**写代码前只需读这一个文件。**
- `scripts/render_preview.py` — PNG 预览渲染脚本，按第 6 步命令行直接调用即可，**无需读取脚本源码**。
- `assets/`（`eg.svg` / `eg2.svg`）— 仅供维护者人工核对的历史示例，**生成新图时不要读取**，会白白增加上下文与推理耗时；所有必要结构/配色/坐标信息 recipe 里都有。
